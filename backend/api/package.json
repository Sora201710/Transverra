{
  "name": "api",
  "version": "1.0.0",
  "description": "An express server that provides an endpoint to handle auth, redirecting tasks to a python celery service, etc. for the Transverra web app.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --trace-sync-io --watch app.mjs",
    "prod": "node app.mjs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1"
  }
}
